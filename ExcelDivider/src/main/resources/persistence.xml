<?xml version="1.0" encoding="utf-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">
    <persistence-unit name="persistenceUnit">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>ru.tsystems.divider.entity.Task</class>

        <class>ru.tsystems.divider.entity.Comment</class>
        <class>ru.tsystems.divider.entity.Consumables</class>

        <class>ru.tsystems.divider.entity.Dates</class>
        <class>ru.tsystems.divider.entity.Epics</class>
        <class>ru.tsystems.divider.entity.Statistics</class>
        <class>ru.tsystems.divider.entity.Workers</class>

        <class>ru.tsystems.divider.entity.Employee</class>
        <class>ru.tsystems.divider.entity.Feature</class>

        <class>ru.tsystems.divider.entity.Nature</class>

        <properties>
            <property name="hibernate.show_sql" value="false"/>
        </properties>

    </persistence-unit>
</persistence>